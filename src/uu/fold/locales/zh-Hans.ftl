fold-about =
    将每个文件（如果没有文件则将标准输入）
    写入标准输出，同时分割长行
fold-usage = fold [选项]... [文件]...
fold-bytes-help = 使用字节而不是列进行计数（这意味着控制字符如换行符不会被特别处理）
fold-spaces-help = 在单词边界处进行断行，而非强制截断
fold-width-help = 使用 WIDTH 作为最大行宽，而非默认值 80
fold-error-illegal-width = 非法的宽度值
fold-error-readline = 读取行失败
